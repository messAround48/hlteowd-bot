services:
  hlteowd-bot:
    image: ghcr.io/messaround48/hlteowd-bot:latest
    container_name: hlteowd-bot
    environment:
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - MT_PROTO_PROXY=${MT_PROTO_PROXY:-}
    restart: unless-stopped
